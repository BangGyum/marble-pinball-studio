# 브라우저 프레임 측정 — 2026-09-09

## 조건과 측정 방법

- 현재 PC의 Codex 내장 브라우저, 실제 게임의 Game / Rankings / UI / CSS 사용.
- 로컬 개발 번들, 1배속, 녹화 꺼짐, 기본 화면 배율, 1명 당첨.
- Canvas 내부 해상도 2240×900, devicePixelRatio 1.75.
- 별도 임시 진입점에서 실제 메서드를 감싸 requestAnimationFrame 간격, frame/render/physics.step 실행 시간을 측정했다. 제품 소스는 수정하지 않았다.
- 각 실행 첫 2초는 프레임 통계에서 제외했다. FPS는 RAF 간격의 평균에서 산출했으며 GPU의 실제 표시 프레임 카운터는 아니다.
- 파이프라인은 각 16초, 분기점은 각 35초 동안 관찰했다. 프레임 통계에는 해당 관찰 시간 내의 완료 후 화면도 포함된다.
- 같은 난수 시드로 출발 조건을 맞췄으나 브라우저/OS/GPU 부하를 고정하지는 못했다. 메서드 래핑 자체에도 작은 측정 오버헤드가 있다.

## 결과

| 맵 / 조건 | 구슬 | 평균 FPS | 프레임 간격 p95 | 최대 프레임 간격 | 25ms 초과 / 측정 프레임 |
|---|---:|---:|---:|---:|---:|
| 파이프라인 기본 | 10 | 59.7 | 16.9ms | 83.9ms | 1 / 836 |
| 파이프라인 기본 | 20 | 60.0 | 16.9ms | 17.4ms | 0 / 841 |
| 파이프라인 기본 | 40 | 60.0 | 16.8ms | 19.8ms | 0 / 840 |
| 파이프라인 발광 생략 | 40 | 60.0 | 16.9ms | 19.3ms | 0 / 841 |
| 파이프라인 Canvas 글자 생략 | 40 | 60.0 | 16.9ms | 18.3ms | 0 / 840 |
| 파이프라인 미니맵 생략 | 40 | 60.0 | 16.9ms | 19.0ms | 0 / 840 |
| 분기점 기본 1차 | 10 | 59.8 | 16.9ms | 83.3ms | 8 / 1974 |
| 분기점 기본 1차 | 20 | 57.8 | 16.9ms | 333.3ms | 23 / 1909 |
| 분기점 기본 1차 | 40 | 17.4 | 208.4ms | 1916.7ms | 248 / 573 |
| 분기점 기본 재실행 | 40 | 60.0 | 16.9ms | 33.1ms | 3 / 1980 |
| 분기점 발광 생략 | 40 | 60.0 | 16.9ms | 20.1ms | 0 / 1981 |
| 분기점 전체 Canvas 그리기 생략 | 40 | 60.0 | 16.8ms | 18.8ms | 0 / 1981 |

분기점 40개 저하 실행에서 frame 호출 시간은 평균 23.35ms, render 평균 7.73ms, 프레임 내 누적 물리 계산은 평균 13.85ms였다. 최대치는 각각 932ms, 247ms, 926ms였다. 최대값들은 동일 프레임이라는 의미가 아니다.

같은 설정의 재실행에서는 frame 평균 1.79ms, render 평균 1.09ms, 프레임 내 물리 계산 평균 0.62ms였다. 추가 측정한 물리 업데이트 1회는 평균 0.68ms, 최대 2.5ms였다.

## 판단

1. 1배속에서도 큰 프레임 지연이 실제로 발생했다. 배속 때문에만 발생하는 문제는 아니다.
2. 동일 40개 설정이 재실행에서 정상으로 돌아왔다. 구슬 수만으로 결정되는 고정적인 성능 한계라고 단정할 수 없다.
3. 지연 실행에서 물리 계산과 렌더링 시간이 모두 크게 늘어났다. 이전 Node 물리 측정만으로 브라우저의 병목을 배제하면 안 된다.
4. 발광 제거로 원인이 해결됐다고 볼 근거는 부족하다. 비교군인 기본 재실행부터 이미 60fps였다.
5. 최초 지연의 원인은 아직 미확정이다. Canvas 처리, GC, 브라우저/OS 스케줄링 및 GPU 경합은 별도의 성능 추적이 필요하다.

## 개선 우선순위

- 지연 프레임의 상세 추적: 물리 업데이트 횟수와 개별 시간, 렌더 구간, long task, 페이지 가시성, GPU/브라우저 성능 추적을 함께 확인한다.
- game.ts의 누적 시간 처리에는 별도 프레임당 계산 예산이 없다. 입력 delta에 0.1초 제한이 있지만 1배속에서도 지연 뒤 최대 약 6회 업데이트(네온 맵에서 약 96회 Box2D Step)가 한 화면 프레임에 몰릴 수 있다. 처리량 상한 및 누적 시간 정책을 설계하면 지연의 연쇄 악화를 줄일 여지가 있다. 단, 시뮬레이션 시간을 임의로 건너뛰지 않도록 주의해야 한다.
- 화면 밖 구슬/레일의 그리기 생략, 고정 미니맵 및 이름/발광 캐싱은 비용을 줄이는 후보이며 이번 간헐적 지연의 해결책으로 검증된 것은 아니다.
- 물리 계산 16분할을 바로 줄이는 것은 벽 관통/정체 회귀 위험이 있어 보류한다.

제품 코드 수정 및 푸시는 하지 않았다. 측정용 소스와 생성 파일은 테스트 후 정리했다.

## 후속 개선 및 브라우저 비교

이후 사용자의 개선 요청으로 game.ts, draw.ts, interaction.cjs를 수정했다.

- 물리 처리에 프레임당 시간 예산(6ms)과 업데이트 횟수 상한을 추가했다. 1배속에서 최대 2회, 배속에 따라 상한을 조정한다. 한 번 시작한 물리 업데이트를 중단하는 제한은 아니다.
- 과부하 때 오래 밀린 실제 시간은 버리고 1/60초 단위 물리 시뮬레이션은 유지한다. 따라서 과부하에서는 실시간 대비 경기가 느려질 수 있다. 충돌 정밀도와 물리 업데이트 순서는 유지한다.
- 메인 화면에 보이지 않는 벽 구간, 장애물, 구슬과 이름을 그리지 않는다. 회전/이동한 벽도 로컬 좌표로 가시성을 판단한다. 미니맵과 편집기는 전체 맵을 표시한다.
- 같은 프레임의 장애물 상태를 메인 화면과 미니맵에서 재사용한다.

현재 PC 내장 브라우저에서 40개, 1배속, 녹화 꺼짐, 각 35초로 이전 소스와 개선 소스를 번갈아 실행했다. 앞선 측정과 마찬가지로 첫 2초를 제외하고 완료 후 화면도 포함했다. Canvas 호출 카운터는 양쪽에 동일하게 적용했다. GPU/OS 부하는 통제하지 않았다.

| 맵 | 버전 | 평균 FPS | 최대 프레임 간격 | 25ms 초과 | 평균 그리기 시간 | 프레임당 평균 Canvas 호출 |
|---|---|---:|---:|---:|---:|---:|
| 분기점 | 이전 | 59.8 | 66.7ms | 2회 | 1.50ms | 1905 |
| 분기점 | 개선 | 59.6 | 83.4ms | 7회 | 1.43ms | 1172 |
| 파이프라인 | 이전 | 59.3 | 133.1ms | 9회 | 1.21ms | 1055 |
| 파이프라인 | 개선 | 59.7 | 50.2ms | 8회 | 0.95ms | 579 |

그리기 호출은 분기점 약 38%, 파이프라인 약 45% 줄었다. 파이프라인의 평균 그리기 시간은 약 21% 줄었다. 순간 끊김은 남았고 분기점에서는 개선 실행의 최악 프레임이 오히려 길었으므로, 간헐적 지연의 완전 해결이나 일관된 FPS 향상으로 해석하면 안 된다.

전체 npm test와 TypeScript 검사를 통과했다. 새 테스트는 긴 지연 후 계산량 상한, 개별 물리 업데이트가 오래 걸릴 때 양보, 화면을 가로지르는 벽과 회전/이동한 벽의 가시성, 미니맵/편집기 전체 표시를 검증한다. 물리 테스트는 기존 맵 완주와 576개 구슬의 벽 이탈 없음도 확인했다.

## 2차 개선: 구슬 및 미니맵 캐시

- 구슬 원, 발광, 테두리, 이름을 투명 OffscreenCanvas에 한 번 그려 재사용한다. 이동은 drawImage 한 번으로 처리한다. 확대율, DPR, 이름, 색상이 바뀌면 다시 만든다. OffscreenCanvas가 없는 브라우저는 기존 벡터 렌더링을 사용한다.
- 미니맵의 영구 고정 지형만 캐시한다. 회전 장애물과 파괴 가능한 장애물은 캐시 위에 실시간으로 그린다. 맵 준비/편집 반영, 크기 및 DPR 변경 시 캐시를 갱신한다.
- 이번 추가 변경에서는 배속, 물리 계산, 충돌 정밀도를 바꾸지 않았다.

실제 게임 UI에 임시 측정 진입점을 붙여 직전 개선 버전과 캐시 버전을 비교했다. 모든 실행은 1배속, 녹화 끔, 30초, 첫 2초 제외이며 이번에는 **running 상태 프레임만** 집계했다. 초기 난수 시드를 맞췄지만 이후 정체 해소 난수 및 프레임 스케줄링까지 동일하게 통제하지는 않았다. Canvas 호출 수는 메인 컨텍스트에 전달되는 메서드 수이며 첫 생성 시 캐시 내부 작업은 포함하지 않는다. 렌더 시간에는 캐시 생성도 포함되나 첫 2초는 워밍업으로 제외된다.

### 1차 측정 (CSS 캔버스 1280 × 514.56, DPR 1.75)

| 맵 / 개수 | 버전 | FPS | 최대 간격 | 25ms 초과 | 평균 렌더 | 최대 렌더 | 평균 호출 |
|---|---|---:|---:|---:|---:|---:|---:|
| 분기점 40 | 직전 | 59.91 | 33.6ms | 5 | 1.14ms | 26.9ms | 1188 |
| 분기점 40 | 캐시 | 59.96 | 28.3ms | 2 | 0.92ms | 9.0ms | 415 |
| 파이프라인 40 | 직전 | 59.92 | 41.6ms | 2 | 0.67ms | 5.0ms | 602 |
| 파이프라인 40 | 캐시 | 55.02 | 1491.5ms | 15 | 1.03ms | 241.8ms | 194 |
| 분기점 100 | 캐시 | 52.72 | 1166.8ms | 59 | 1.74ms | 197.7ms | 559 |
| 파이프라인 100 | 캐시 | 57.74 | 116.6ms | 37 | 1.09ms | 30.6ms | 331 |

이 실행에서는 중간에 화면 확인용 스크린샷을 찍었다. 스크린샷/GPU 읽기 작업이 측정에 영향을 줄 가능성도 배제하지 못하므로, 재측정에서는 스크린샷과 테스트/빌드를 동시에 실행하지 않았다. 큰 지연의 원인이 스크린샷이라는 증거는 없다.

### 재측정 (CSS 캔버스 711.43 × 646, DPR 1.75)

창 크기가 달라져 1차와 절대값을 직접 비교하면 안 된다. 아래 표 내부의 전후 비교는 같은 크기이며, 파이프라인은 캐시→직전 순서로 바꿨다. 추가로 각 Canvas 메서드 소요 시간을 계측해 20ms 초과 호출을 기록했다. 양쪽 모두 그러한 단일 호출은 없었다.

| 맵 / 개수 | 버전 | FPS | 최대 간격 | 25ms 초과 | 평균 렌더 | 렌더 p95 | 평균 호출 |
|---|---|---:|---:|---:|---:|---:|---:|
| 파이프라인 40 | 캐시 | 59.96 | 33.5ms | 4 | 0.88ms | 1.7ms | 206 |
| 파이프라인 40 | 직전 | 60.00 | 21.7ms | 0 | 1.19ms | 2.1ms | 613 |
| 분기점 100 | 직전 | 60.00 | 21.3ms | 0 | 1.67ms | 3.0ms | 1426 |
| 분기점 100 | 캐시 | 60.00 | 25.2ms | 2 | 1.32ms | 2.4ms | 564 |

재측정 평균 렌더 시간은 파이프라인 약 26%, 분기점 약 21% 감소했다. 메인 Canvas 호출은 각각 약 66%, 60% 줄었다. 그러나 캐시 버전에서도 순간 지연이 남았고 1차에서는 1초 이상 지연이 있었으므로, 렉의 완전 해결 또는 일관된 최악 프레임 개선으로 해석하면 안 된다. 이 측정은 RAF/JS 시간이며 GPU 실제 화면 표시 시간을 직접 측정하지 않았다.

회귀 테스트는 이동 중 재사용, 확대율/DPR/이름/색상 갱신, 맵 재준비 초기화, 회전 각도 갱신, 파괴 장애물 제거, 미지원 브라우저의 벡터 경로를 검증한다.

전체 npm test 및 npm run build를 통과했다. 실제 로컬 게임의 확대 후 한글 이름·발광·미니맵 표시를 확인하고 기본 배율로 복원했다. 임시 비교 소스 3개와 생성 폴더를 제거하고 측정 서버를 중지했다. 정상 개발 서버(43187)는 유지했다. 아직 커밋/푸시는 하지 않았다.

## 3차 개선: 200개 구슬의 순위표 갱신

사용자의 200개 최적화 요청에 따라 실제 내장 브라우저에서 파이프라인/분기점을 각각 40초씩 전후 비교했다. 1배속, 녹화 꺼짐, DPR 1.75, CSS 캔버스 약 1280 × 514.57이다. 첫 2초를 제외한 running 상태의 RAF 간격, Game.render, Box2dPhysics.step, Rankings.update를 측정했다. 메인 Canvas 함수별 프록시는 사용하지 않았다. 스크린샷/빌드/물리 테스트는 측정 중 실행하지 않았다.

### 원인과 적용 내용

- 기준 측정의 물리 계산은 평균 4.32~5.15ms/프레임, p95 9.7~9.9ms였다. 순위표도 300ms마다 평균 4.80~6.34ms를 사용했다.
- 순위가 바뀔 때마다 200개 행 전체를 replaceChildren으로 다시 붙이던 작업을, 순서가 어긋난 행만 insertBefore로 옮기도록 변경했다. 순위/도착 여부/접근성 라벨/숨김 상태도 값이 바뀔 때만 쓴다. 전원 표시와 갱신 주기는 유지한다.
- 보강벽과 겹치는 edge fixture 제거도 시도했으나, 기존 pipeline-run의 범퍼 접촉 비율 테스트를 통과하지 못해 **완전히 되돌렸다**. 테스트 기준을 낮추지 않았다. 최종 physics-box2d.ts는 이전과 내용이 같다. 물리 시간 간격, 서브스텝, 반복 횟수, 충돌 형상은 변경하지 않았다.

| 맵 (200개) | 버전 | 평균 FPS | 25ms 초과 / 프레임 수 | 최대 간격 | 순위표 평균 | 순위표 p95 | render 평균 | physics 평균 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 파이프라인 | 직전 | 59.49 | 21 / 2260 | 33.6ms | 4.80ms | 8.6ms | 1.10ms | 4.32ms |
| 파이프라인 | 개선 | 59.69 | 11 / 2268 | 66.6ms | 1.99ms | 6.3ms | 0.85ms | 3.56ms |
| 분기점 | 직전 | 59.24 | 28 / 2252 | 50.0ms | 6.34ms | 10.3ms | 1.33ms | 5.15ms |
| 분기점 | 개선 | 59.97 | 1 / 2279 | 33.4ms | 2.73ms | 7.2ms | 1.05ms | 3.70ms |

순위표 평균 비용은 각각 약 59%, 57% 줄었다. 이번 실행에서는 긴 프레임 수도 감소했지만 파이프라인 최대 간격은 오히려 커졌으므로 모든 끊김이 제거됐다고 볼 수 없다. 물리 코드는 변경하지 않았으므로 표의 physics 시간 감소를 물리 최적화 효과로 주장하지 않는다.

초기 난수 시드는 같게 설정했지만, 재사용한 Box2D 월드의 트리 상태/충돌 처리 순서와 프레임 스케줄링은 통제하지 않았다. 실제 경로/완주 진행은 달랐다(40초 시점 파이프라인 전후 199명 도착, 분기점 166→185명). 따라서 전후 물리 접촉량도 다르며 이는 완전히 동일한 작업량의 마이크로벤치마크가 아니다. 순위표 시간은 이벤트 호출 자체의 시간이고 프레임 수/CPU 시간과 별도로 집계했다. GPU 실제 표시 시간이나 외부 OS 부하는 직접 측정하지 않았다.

새 테스트는 단일 추월에서 행 하나만 이동, 변동 없는 순위에서 행 이동 및 접근성 속성 쓰기 없음, 기존 도착 순위/표시 유지, 300명 순위의 반복 재정렬을 확인한다.

최종 npm test와 npm run build가 통과했다. 72개 회전 위상/576개 구슬 벽 관통 검사와 기존 범퍼 접촉 검사를 그대로 통과했다. 브라우저에서 200명 실시간 순위표의 표시도 확인했다. 임시 계측 파일 4개와 생성 폴더, 측정 서버는 정리했고 정상 로컬 서버는 유지한다. 이번 변경은 커밋/푸시하지 않았다.
